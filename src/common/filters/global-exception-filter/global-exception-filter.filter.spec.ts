import { GlobalExceptionFilterFilter } from './global-exception-filter.filter';

describe('GlobalExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new GlobalExceptionFilterFilter()).toBeDefined();
  });
});
